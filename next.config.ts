
/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // NOTE: This project is currently running with Vite for the AI Studio preview environment.
  // This file is present to match the requested Next.js project structure.
};

export default nextConfig;
